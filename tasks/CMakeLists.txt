# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(iodrivers_baseTaskLib)
ADD_LIBRARY(${IODRIVERS_BASE_TASKLIB_NAME} SHARED 
    PortStream.cpp ConfigureGuard.cpp ${IODRIVERS_BASE_TASKLIB_SOURCES})

add_dependencies(${IODRIVERS_BASE_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${IODRIVERS_BASE_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${IODRIVERS_BASE_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${IODRIVERS_BASE_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${IODRIVERS_BASE_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${IODRIVERS_BASE_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${IODRIVERS_BASE_TASKLIB_HEADERS} ConfigureGuard.hpp
    DESTINATION include/orocos/iodrivers_base)

